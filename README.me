# Agent Ecosystem - 多Agent协作的生态智能系统

## 项目简介

Agent Ecosystem是一个基于多Agent协作的生态智能系统，旨在通过共享世界模型、消息总线和技能图谱，实现从单体智能到生态智能的升级。

## 核心功能

- **多Agent协作**：支持DesignAgent、DevelopmentAgent和TestingAgent等多种智能Agent协同工作
- **共享世界模型**：所有Agent共享同一个语义宇宙，实现知识和经验的共享
- **技能图谱**：技能在Agent间自由流动和继承，实现能力的快速传播
- **消息总线**：基于事件驱动的通信机制，实现Agent间的高效协作
- **API接口**：提供RESTful API接口，方便外部系统集成
- **前端界面**：现代化的Web界面，支持任务提交和系统状态查看

## 系统架构

```
┌──────────────────────────────────────────────────────────┐
│                      Agent Ecosystem                    │
├──────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │Design   │  │Development│  │Testing  │  │...       │ │
│  │Agent    │  │Agent     │  │Agent    │  │          │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│          │          │          │          │              │
│          ▼          ▼          ▼          ▼              │
│  ┌──────────────────────────────────────────────────┐     │
│  │                 消息总线 (Message Bus)          │     │
│  └──────────────────────────────────────────────────┘     │
│                          │                               │
│                          ▼                               │
│  ┌──────────────────────────────────────────────────┐     │
│  │              共享世界模型 (Shared Memory)        │     │
│  └──────────────────────────────────────────────────┘     │
│                          │                               │
│                          ▼                               │
│  ┌──────────────────────────────────────────────────┐     │
│  │                技能图谱 (Skill Graph)           │     │
│  └──────────────────────────────────────────────────┘     │
│                          │                               │
│                          ▼                               │
│  ┌──────────────────────────────────────────────────┐     │
│  │                   Kernel 核心服务                │     │
│  └──────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

## 技术栈

- **后端**：Python 3.14+
- **前端**：HTML5, CSS3, JavaScript
- **API**：FastAPI
- **数据库**：MongoDB, Neo4j, PostgreSQL
- **前端存储**：IndexedDB
- **AI模型**：DeepSeek, ApiYi, ToAPIs, 讯飞星火, 智谱GLM-5, 百川
- **外部服务**：Google搜索, SerpApi, 新闻API, 百度地图, 高德地图

## 快速开始

### 环境要求

- Python 3.14+
- 安装依赖：`pip install -r requirements.txt`

### 配置

1. 复制 `.env.example` 为 `.env`
2. 填写相关API密钥和配置信息

### 启动服务

```bash
# 启动API服务
python agent-ecosystem/api.py

# 启动前端预览服务器
python agent-ecosystem/frontend/server.py --port 8080
```

### 访问

- **前端界面**：http://localhost:8080
- **API文档**：http://localhost:8000/docs
- **健康检查**：http://localhost:8000/health

## API接口

### 提交任务

```bash
POST /tasks
Content-Type: application/json

{
  "id": "task-001",
  "type": "web_design",
  "content": "设计一个电商网站首页",
  "requirements": {
    "style": "现代简约",
    "color": "蓝色系",
    "features": ["商品展示", "购物车", "用户登录"]
  }
}
```

### 健康检查

```bash
GET /health
```

### 获取系统信息

```bash
GET /system
```

## 项目结构

```
agent-ecosystem/
├── agents/          # 智能Agent实现
├── collaboration/   # 协作服务
├── frontend/        # 前端界面
├── kernel/          # 核心服务
├── memory/          # 记忆服务
├── message_bus/     # 消息总线
├── runtime/         # 执行服务
├── scheduler/       # 调度服务
├── shared_memory/   # 共享世界模型
├── skill_graph/     # 技能图谱
├── tool/            # 工具服务
├── api.py           # API服务
├── config.py        # 配置文件
└── main.py          # 主入口
```

## 许可证

MIT License

## 贡献

欢迎贡献代码和提出问题！
